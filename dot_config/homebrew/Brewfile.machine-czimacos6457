# Reference: https://github.com/Homebrew/homebrew-bundle
#
# MACHINE LAYER: czimacos6457
#
# Applied only on this machine (D3, three-layer model: base -> role -> machine).
# Deploys to ~/.config/homebrew/Brewfile.machine-czimacos6457, but only on that
# machine — .chezmoiignore filters out every non-matching machine layer.
#
# Keep this file for things tied to THIS physical machine. Employer tooling that
# any CZI Mac would want belongs in Brewfile.role-work instead; the CZI and cloud
# CLIs originally landed here only because the role layer did not exist yet (R3).

# Brightness/volume control for external displays — specific to this desk setup,
# not something every work machine needs.
cask "monitorcontrol"
